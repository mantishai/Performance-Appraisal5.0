-- =============================================================
-- 更新admin用户密码为 '1'
-- =============================================================

USE hr_system;

-- 更新admin密码
UPDATE user SET password = '1' WHERE username = 'admin';

-- 验证更新
SELECT id, username, real_name, role, status FROM user WHERE username = 'admin';

-- =============================================================
-- 完成！
-- 使用用户名: admin
-- 密码: 1
-- =============================================================
